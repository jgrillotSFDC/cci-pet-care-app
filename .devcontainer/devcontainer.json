{
  "name": "Pet Care App Dev Container",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "features": {},
  "containerEnv": {
    "SF_TEMP_SHOW_SECRETS": "true"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "salesforce.salesforcedx-vscode",
        "ms-python.python"
      ]
    }
  },
  "remoteUser": "root",
  "postCreateCommand": "bash -lc 'source /etc/profile.d/cumulusci-key.sh && sf --version && sf plugins --core && cci version'"
}
