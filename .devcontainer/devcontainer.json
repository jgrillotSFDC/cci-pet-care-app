{
  "name": "Pet Care App Dev Container",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "features": {},
  "customizations": {
    "vscode": {
      "extensions": [
        "salesforce.salesforcedx-vscode",
        "ms-python.python"
      ]
    }
  },
  "remoteUser": "root",
  "postCreateCommand": "sf --version && sf plugins --core && cci version"
}
